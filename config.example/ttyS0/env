# Путь к устройству, которое будет прокидываться по tcp (например, /dev/ttyS0 - это COM1)
SERCD_DEVICE=/dev/ttyS0

# Хост, который будет слушать демон. Можно указать адрес конкретного интерфейса, чтобы слушать только на нём, либо 0.0.0.0 - все интерфейсы
SERCD_HOST=0.0.0.0

# Порт, который будет слушать демон
SERCD_PORT=7000
